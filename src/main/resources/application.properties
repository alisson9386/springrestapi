spring.datasource.url=jdbc:postgresql://localhost:5432/api-rest-spring
spring.datasource.username=postgres
spring.datasource.password=Z0rr0b@tmak
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
server.servlet.context-path=/springrestapi

#Número de milisegundos maximo que o cliente aguardara conexão
spring.datasource.hikari.connection-timeout=20000

#Mínimo de conexões inativas mantidas
spring.datasource.hikari.minimim-idle=10

#Máximo do pool de conexão
spring.datasource.hikari.maximum-pool-size=40

#Tempo ociosos para conexão
spring.datasource.hikari.idle-timeout=10000

#Salvando no banco automaticamente
spring.datasource.hikari.auto-commit=true